# AskUserQuestion Padding

When using AskUserQuestion, always end your text with a horizontal rule (`---`) followed by two blank lines before the tool call. The AskUserQuestion widget overlaps the last ~2 lines of output, so the padding gets hidden instead of content.
